# Aegis —— 现代套壳浏览器（安全加固版）
# Python 3.10+
# 内核：PySide6 + QtWebEngine（随附 Chromium 版本由 Qt 运行时决定，不臆造）

# ---- 核心（Qt 6 官方绑定 PySide6，含 QtWebEngine）----
# 锁版本：避免上游破坏性变更与供应链漂移
# 6.11.1 捆绑的 Chromium 接近 140（2026 水平），显著新于 6.7.3 的 118
PySide6==6.11.1

# ---- 密码加密（硬依赖：缺失即视为"密码功能不可用"，UI 明确标红）----
# v2.1.2：cryptography 由 43.0.3 提级到 50.0.0 —— 43.x 线已累积多个
# 上游安全修复（含 X.509/PEM 解析类缺陷），安全敏感依赖不锁在旧线上。
cryptography==50.0.0
keyring==25.4.1
# Windows 正式入口：pywebview + WebView2；bottle 是桥接服务依赖。
pywebview==5.4
bottle==0.13.2

# ---- 打包（仅打包时安装；与运行依赖分离）----
PyInstaller==6.10.0

# 说明：运行依赖必须全部安装，否则启动即自检失败（不做静默降级）。
# 锁定次要版本以平衡可复现性与安全补丁获取。

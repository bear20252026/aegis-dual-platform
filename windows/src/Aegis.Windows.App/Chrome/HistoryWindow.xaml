<Window x:Class="Aegis.Windows.Chrome.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="历史记录" Width="820" Height="620"
        Background="{DynamicResource ChromeBackgroundBrush}"
        FontFamily="Segoe UI, Microsoft YaHei UI"
        WindowStartupLocation="CenterOwner">
  <Window.Resources>
    <!-- ── 深浅主题色板（与主窗口同 key——随设置切换） ── -->
    <SolidColorBrush x:Key="ChromeBackgroundBrush" Color="#FF101827"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FF1B2537"/>
    <SolidColorBrush x:Key="CardBrush" Color="#FF1F2A3D"/>
    <SolidColorBrush x:Key="CardHoverBrush" Color="#FF2A3A55"/>
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#B3FFFFFF"/>
    <SolidColorBrush x:Key="TextMutedBrush" Color="#FF8A93A6"/>
    <SolidColorBrush x:Key="FieldBackgroundBrush" Color="#FF1B2537"/>
    <SolidColorBrush x:Key="FieldBorderBrush" Color="#FF2E3B55"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#FF3B82F6"/>
    <SolidColorBrush x:Key="AccentSoftBrush" Color="#223B82F6"/>
    <SolidColorBrush x:Key="DangerBrush" Color="#FF7F1D1D"/>

    <!-- ── 主按钮 ── -->
    <Style x:Key="PrimaryButton" TargetType="Button">
      <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
      <Setter Property="Foreground" Value="White"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="32"/>
      <Setter Property="Padding" Value="14,0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" CornerRadius="10" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.88"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="Opacity" Value="0.72"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── 幽灵按钮（删除/次要） ── -->
    <Style x:Key="GhostButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="8,4"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoftBrush}"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── 快捷日期芯片（ToggleButton） ── -->
    <Style x:Key="ChipToggle" TargetType="ToggleButton">
      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="30"/>
      <Setter Property="Padding" Value="14,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Border x:Name="Bd" CornerRadius="15" Background="Transparent"
                    BorderBrush="{DynamicResource FieldBorderBrush}" BorderThickness="1" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter TargetName="Bd" Property="BorderBrush" Value="Transparent"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ── 日期选择器（可展开日历）── -->
    <Style TargetType="DatePicker">
      <Setter Property="Height" Value="32"/>
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Background" Value="{DynamicResource FieldBackgroundBrush}"/>
      <Setter Property="BorderBrush" Value="{DynamicResource FieldBorderBrush}"/>
    </Style>

    <!-- ── 日期分组头：日期 + 星期 + 条数徽标 ── -->
    <DataTemplate x:Key="DayHeaderTemplate">
      <Border CornerRadius="12" Margin="0,14,0,0" Padding="14,10"
              Background="{DynamicResource AccentSoftBrush}" BorderBrush="{DynamicResource AccentBrush}"
              BorderThickness="1">
        <DockPanel>
          <Border DockPanel.Dock="Right" CornerRadius="10" Padding="10,2" Background="{DynamicResource AccentBrush}">
            <TextBlock Text="{Binding CountText}" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
          </Border>
          <StackPanel>
            <TextBlock Text="{Binding DisplayDate}" Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="14" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding Weekday}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,1,0,0"/>
          </StackPanel>
        </DockPanel>
      </Border>
    </DataTemplate>

    <!-- ── 单条历史：时刻 + 标题 + 域名 + 悬停删除 ── -->
    <DataTemplate x:Key="HistoryRowTemplate">
      <Border x:Name="Row" CornerRadius="10" Margin="0,6,0,0" Padding="12,9"
              Background="{DynamicResource CardBrush}">
        <Border.Effect>
          <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.25" Color="#000000"/>
        </Border.Effect>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <Border Grid.Column="0" Width="46" Height="24" CornerRadius="12" Margin="0,0,12,0"
                  Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Center">
            <TextBlock Text="{Binding TimeText}" Foreground="{DynamicResource AccentBrush}"
                       FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="{Binding Host}" Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,2,0,0"/>
          </StackPanel>
          <Button Grid.Column="2" Content="✕" Style="{StaticResource GhostButton}"
                  Tag="{Binding Id}" Click="Delete_Click" ToolTip="删除这条记录"
                  Margin="6,0,0,0" VerticalAlignment="Center"/>
        </Grid>
      </Border>
    </DataTemplate>
  </Window.Resources>

  <Grid Margin="22">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- ══ 标题行 ══ -->
    <DockPanel Grid.Row="0">
      <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
        <TextBlock Text="历史记录" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="20" FontWeight="SemiBold"/>
        <TextBlock x:Name="SummaryText" VerticalAlignment="Bottom" Margin="12,0,0,3"
                   Foreground="{DynamicResource TextMutedBrush}" FontSize="12"/>
      </StackPanel>
      <Button DockPanel.Dock="Right" Content="清除全部" Style="{StaticResource GhostButton}"
              Background="{DynamicResource DangerBrush}" Foreground="White" Click="Clear_Click"
              ToolTip="清空全部历史（不可恢复）"/>
    </DockPanel>

    <!-- ══ 搜索行 ══ -->
    <Border Grid.Row="1" Margin="0,14,0,0" Height="36"
            Background="{DynamicResource FieldBackgroundBrush}" BorderBrush="{DynamicResource FieldBorderBrush}"
            BorderThickness="1" CornerRadius="18">
      <Grid>
        <TextBlock Text="🔍" Margin="14,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextMutedBrush}"/>
        <TextBlock x:Name="SearchHint" Text="搜索历史（地址 / 标题）…" Margin="40,0,0,0" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextMutedBrush}" IsHitTestVisible="False"/>
        <TextBox x:Name="SearchBox" Margin="34,0,14,0" BorderThickness="0" Background="Transparent"
                 Foreground="{DynamicResource TextPrimaryBrush}" CaretBrush="{DynamicResource TextPrimaryBrush}"
                 VerticalContentAlignment="Center" Height="32" TextChanged="SearchBox_TextChanged"/>
      </Grid>
    </Border>

    <!-- ══ 日期筛选行 ══ -->
    <WrapPanel Grid.Row="2" Margin="0,10,0,0">
      <ToggleButton x:Name="ChipAll" Style="{StaticResource ChipToggle}" Content="全部"
                    IsChecked="True" Checked="ChipFilter_Changed"/>
      <ToggleButton x:Name="ChipToday" Style="{StaticResource ChipToggle}" Content="今天" Margin="6,0,0,0"
                    Checked="ChipFilter_Changed"/>
      <ToggleButton x:Name="ChipYesterday" Style="{StaticResource ChipToggle}" Content="昨天" Margin="6,0,0,0"
                    Checked="ChipFilter_Changed"/>
      <ToggleButton x:Name="ChipWeek" Style="{StaticResource ChipToggle}" Content="近7天" Margin="6,0,0,0"
                    Checked="ChipFilter_Changed"/>
      <ToggleButton x:Name="ChipMonth" Style="{StaticResource ChipToggle}" Content="本月" Margin="6,0,0,0"
                    Checked="ChipFilter_Changed"/>
      <TextBlock Text="自定义：" VerticalAlignment="Center" Margin="14,0,6,0"
                 Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
      <DatePicker x:Name="CustomDate" Width="140" SelectedDateChanged="CustomDate_SelectedDateChanged"
                  ToolTip="展开日历选择某一天"/>
      <ToggleButton x:Name="ChipRange" Style="{StaticResource ChipToggle}" Content="范围" Margin="8,0,0,0"
                    Checked="ChipFilter_Changed" Unchecked="ChipFilter_Changed" ToolTip="按起止日期范围查询"/>
      <StackPanel x:Name="RangePanel" Orientation="Horizontal" Visibility="Collapsed" Margin="8,0,0,0">
        <DatePicker x:Name="RangeFrom" Width="130" SelectedDateChanged="RangeDate_Changed" ToolTip="开始日期"/>
        <TextBlock Text="—" VerticalAlignment="Center" Margin="6,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <DatePicker x:Name="RangeTo" Width="130" SelectedDateChanged="RangeDate_Changed" ToolTip="结束日期"/>
      </StackPanel>
    </WrapPanel>

    <!-- ══ 分组列表 ══ -->
    <TextBlock x:Name="EmptyHint" Grid.Row="3" Margin="0,18,0,0" FontSize="13"
               Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center"/>
    <ScrollViewer Grid.Row="3" Margin="0,8,0,0" VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
      <ItemsControl x:Name="HistoryList" Margin="0,0,8,0"/>
    </ScrollViewer>
  </Grid>
</Window>
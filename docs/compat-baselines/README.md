# WebView2 兼容性回归基线

由 .github/workflows/compat.yml 每周一 06:00 UTC 自动生成：
- probe-latest.json：最新探测报告（Runtime 版本 + 关键 API 可用性）
- probe-YYYYMMDD.json：按日期归档的历史基线

用途：Evergreen Runtime 2 周更新节奏下，比对"版本 × 功能"回归基线。

# 修复执行日志（对应 full-audit-2026-09-04.md）

> 本文档是修复工作的**主控记录**：每个修复项列明层次、原文件、改动内容、验证方式与提交号；「界限」一节明确本次**不做**的项，防止范围蔓延。随修复推进持续更新，最终状态 = 完整修改历史。

## 批次 1（本次执行）：安全止血（审计「立即」清单）

### 层次 A —— Windows 现役栈（legacy/windows-pywebview）

| # | 审计项 | 原文件 | 修复内容 | 状态 |
|---|---|---|---|---|
| A1 | P0-1/P0-2 加固层死代码 + window.open 绕过门禁 | `app/shell_adapter.py`、`main_webview.py`、`app/webview2_probe.py` | ① `core()` 解析路径改为 `window.native.webview.CoreWebView2`（pywebview 6.2.1 实测布局），单源函数 `resolve_core()` 收敛全仓 10 处坏解析；② 新增 `wait_for_core()`，全部原生挂接移到 `shell.start(func)` 回调中窗口/WebView2 就绪后执行（原时序在 start 前，`window.native` 尚不存在）；③ 类级替换 pywebview `EdgeChrome.on_new_window_request`——新窗口 URI 强制过 `is_navigation_safe` 门禁（fail-closed），先于实例创建、无竞态；④ 关键降级点由 `except: pass` 改为显式 `log_event` | 完成 |
| A2 | P0-3 下载静默禁用（用户无感知） | `main_webview.py` | 订阅 `CoreWebView2.DownloadStarting`（pywebview 仍按其默认取消下载），经 NavQueue 向用户显示明确提示「当前版本不支持下载」，安全日志留痕。完整下载管理器 = 批次 3 | 完成 |
| A3 | P0-4 `current_url`/`js_error` 远程可达 | `app/api_bridge.py`、`app/bridge/navigation.py` | `current_url` 移出 `_JS_EXPOSED`（全仓 grep 证实零 JS 消费，仅内部 Python 调用）；`js_error` 保留暴露（工具栏/start.html 合法消费）但方法内加 `_check_trusted_source` 门禁——远程页面调用不再记录（同时关闭 A2 检查的「空 source 绕过」） | 完成 |

**A1 激活风险边界**（激活死代码=行为变化，逐项裁决）：
- `IsWebMessageEnabled=False` **不全局启用**——实测 pywebview 6.2.1 的 js_api 传输依赖 `WebMessageReceived`（`on_script_notify`）且自行设 True，全局关闭会断桥。远端页面禁用由既有 per-origin 请求翻转承担；修正 `_apply_webview2_settings` 的错误 docstring。
- `AreHostObjectsAllowed=False`、`AreDefaultScriptDialogsEnabled=False`：pywebview 未用宿主对象（grep 无 AddHostObjectToScript）、弹窗走自绘 → 启用。
- ESM（`_apply_enhanced_security`）：按项目既定 auto 模式探测启用，启用/跳过均显式日志。
- probe/监控/预热/缓存上限：只读或默认 no-op（http_cache_mb 默认 0），启用无风险。

### 层次 B —— Android（android/app）

| # | 审计项 | 原文件 | 修复内容 | 状态 |
|---|---|---|---|---|
| B1 | P0-5 配置变更销毁全部 WebView | `MainActivity.kt`、`BrowserViewModel.kt` | `onDestroy` 仅在 `isFinishing` 时 `suspendAll`+tearDown；Activity 经 `attachActivity/detachActivity` 向 ViewModel 提供宿主引用 | 完成 |
| B2 | P0-6 崩溃恢复用 ApplicationContext 建 WebView | `BrowserViewModel.kt` | `rebuildAfterRendererGone` 优先用宿主 Activity context（无则回退 appContext 并留痕） | 完成 |
| B3 | P2-1 SSL/加载错误静默白屏 | `AegisWebViewClient.kt`、`BrowserViewModel.kt`、`MainActivity.kt` | 覆写 `onReceivedError`（主框架）/`onReceivedHttpError`（主框架）/`onReceivedSslError`（**保持默认取消，绝不 proceed**），错误状态入 StateFlow，UI 显示错误面板 + 重试；新导航开始时清除 | 完成 |
| B4 | P1-1 AegisBridge 注入所有页面 | `AegisHomeBridge.kt`、`SecureWebViewFactory.kt` | 桥方法执行前校验宿主 WebView 当前 URL 仅限本地壳页（`file:///android_asset/`、about:blank），远端页面调用一律拒绝并留痕 | 完成 |
| B5 | P2-5 下载文件名未净化/危险扩展漏判 | `WebViewDownloadHandler.kt`、`DownloadPolicy.kt` | 文件名净化（剥离路径段/`..`/控制字符/尾点，回退默认名）；危险扩展判定改用「净化后文件名 ∪ URL 去查询串路径段」。下载走 broker 的完整改造依赖契约新增动作类型 = 批次 2 | 完成 |

### 层次 C —— C# 目标栈（windows/src）

| # | 审计项 | 原文件 | 修复内容 | 状态 |
|---|---|---|---|---|
| C1 | P1 下载绕过 Broker | `WebView/HostWebView.cs`、`Broker/BrowserPolicyBroker.cs` | 订阅 `DownloadStarting`：无 AuthorizedAction → `Handled=true` fail-closed 拒绝 + 审计留痕，使代码与「无 AuthorizedAction 不能下载」声明一致 | 完成 |

## 批次 2/3（本次明确不做——界限）

1. threat_feed 刷新接线或删除声明（P1-4）
2. Android http/https intent-filter 消费（onNewIntent）（P1-4）
3. `localhost:8000` 分类修复、iframe `isForMainFrame` 区分、HTTPS 升级大小写（P1-6/P1-2/P1-3）
4. Rust canonical JSON 转义 + signatures 剔除对齐 + golden 向量；C#/Kotlin 纳入共享向量差分
5. Windows 下载完整 UI、书签「收藏」按钮、历史管理 UI、设置界面
6. config 影子字段清理（30+）、死代码群删除（mcp/tab_state/VerticalTabBar 等）
7. Android 进程死亡恢复（onSaveInstanceState）、下载走 broker 契约改造
8. 双栈退役 ADR、SECURITY.md 完整化、redteam 实弹化
9. 切标签保活（Windows）、标签标题/进度反馈、性能项（子资源回调、ACL 放大）

## 修改历史（提交记录，分支 `fix/audit-2026-09-04`）

| 提交 | 内容 | 涉及文件 |
|---|---|---|
| `4af825f` | docs: 全面审计报告 + 本修复日志 | `docs/quality-reports/full-audit-2026-09-04.md`、`fix-log-2026-09-04.md` |
| `1dc9d1b` | A1+A2：原生加固层复活（解析路径/时序/新窗口门禁/下载提示） | `app/shell_adapter.py`、`main_webview.py`（797→267 行）、新增 `app/native_hardening.py`(435)/`app/native_interception.py`(196)/`app/native_monitoring.py`(115)、`app/webview2_probe.py`、新增 `selftest_native_core.py`、`.github/workflows/ci.yml` |
| `b442e57` | A3：current_url 移出白名单 + js_error 受信门禁 | `app/api_bridge.py`、`app/bridge/navigation.py`、`selftest_api_bridge.py` |
| `14df29f` | C1：C# 下载经 broker fail-closed 拒绝并审计 | `windows/.../WebView/HostWebView.cs`、`Broker/BrowserPolicyBroker.cs`、`tests/.../BrowserPolicyBrokerTests.cs` |
| `af4ba4b` | B1-B5：Android 生命周期/崩溃恢复/错误页/桥门禁/下载净化 | `MainActivity.kt`、`BrowserViewModel.kt`、`AegisWebViewClient.kt`、`AegisHomeBridge.kt`、`SecureWebViewFactory.kt`、`WebViewDownloadHandler.kt`、`DownloadPolicy.kt`、`UiColors.kt` |
| `3de786e` | docs: CHANGELOG + CLAUDE/README 自检清单同步 + 本日志收口 | `CHANGELOG.md`、`CLAUDE.md`、`README.md`、本文件 |
| `d1d6775` | CI 修复①：B604 误报（`shell=` 壳抽象参数随迁 app/ 进入扫描范围）→ 参数更名 `shell_adapter`，不加豁免 | `legacy/windows-pywebview/app/native_hardening.py` |
| `0606bdb` | CI 修复②：UI 回归测试对齐 A8 拆分后单源目录（**master 存量红**，非本分支引入；touchmove/snakeBest/capability → start.snake.js，CSS 断言 → start.css，语法检查改逐块解析） | `tests/ui-regression/start_page.test.mjs` |

> PR：[#15](https://github.com/bear20252026/aegis-dual-platform/pull/15)（fix/audit-2026-09-04 → master）。首轮 CI 14/16 过（Python job 挂 B604 为本分支搬移引入；UI Regression 为 master 存量红），补 2 提交后 **18/18 全绿**。已 squash 合并为 master `d1ffc0a`。

---

## 批次 2/3（第二轮执行：威胁面收口 + 产品可用性）

> 分支 `fix/audit-batch2-2026-09-04`。**第 0 步真机验证先于一切**——批次 1 激活的代码在真机冒烟中暴露挂死问题（见 W4），修复后全链路确认生效。

| # | 审计项 | 原文件 | 修复内容 | 状态 |
|---|---|---|---|---|
| W4 | 批次1挂接真机挂死 | `main_webview.py`、`app/native_interception.py`、`app/shell_adapter.py` | 真机三轮定位：① start(func) 线程触碰 .NET 属性跨 STA 阻塞；② loaded 线程同样非 UI 线程（异常实锤 "CoreWebView2 can only be accessed from the UI thread"）。挂接点改 **CoreWebView2InitializationCompleted**（.NET 保证 UI 线程、早于首文档——FIX-1 对首文档生效）；FIX-1 候选探测（pythonnet 暴露名差异）；DownloadStarting 改 `+=` 订阅；删无消费者的 wait_for_core。真机终验：门禁/注入/下载提示/崩溃监听/收紧全生效 | 完成 |
| W5 | 批次2-1 threat_feed 只读不更 | `main_webview.py`、`app/bridge_hooks.py` | 启动快照写 `api._blocked_hosts` + threat_feed_url 非空时后台 refresh（https 强制/5MB/原子落盘），成败留痕；未配置诚实提示 | 完成 |
| W6 | 批次2-2 页内点击绕过拦截 | `app/fingerprint_pipeline.py` | 链接拦截 JS 嵌入黑名单快照客户端判定（命中阻止+toast；window.open 同门禁）；修 startsWith('#') 恒假（锚点用 getAttribute） | 完成 |
| W7 | 批次3-8 书签无添加入口 | `app/bridge/bookmarks.py`、`app/api_bridge.py`、`app/shell_toolbar.py`、`shared/shell/start.html` | 工具栏 ☆ 按钮 + toggle_bookmark 桥（零 JS 可控参数、URL 服务端取、仅 http/https；威胁等价分析见 docstring）；start.html 误导文案修正；selftest 行为回归 | 完成 |
| W8 | 批次3-10 下载决策（文档） | `docs/product/supported-features.md` | 文档重写对齐现实（修正审计点名的漂移：已实现功能被列「不做」、不存在的状态机被宣称）+ Windows 下载明示不支持 + 诚实缺口清单 | 完成 |
| T1-T3 | Android 地址栏三连 | `SearchEngines.kt`、`OriginPolicy.kt`、`AndroidBroker.kt`、`AegisWebViewClient.kt` | host:port 误杀（prefix 含点/纯数字端口段→DOMAIN；实证修正 192.168.1.1:8080 原本正常）；about:blank 决策矛盾（精确放行 + canonicalOrigin 防 NPE）；HTTPS 升级大小写（IGNORE_CASE 替换） | 完成 |

### 批次 2/3 验证记录

- Windows：ruff/bandit/mypy（9 文件）0 错误；8 selftest + ui-regression 10/10 全过；真机冒烟 threat 快照与挂接日志确认
- Android：:app/:broker 单测 + :app/:webview-adapter ktlint 全过（SearchEnginesTest +4、OriginPolicyTest +2）
- Android 诚实声明：T3（upgradeToHttpsIfNeeded）在 webview-adapter 模块——该模块 0 测试（审计已知缺口），行为由代码审查保证，真机回归待做

## 修改历史（批次 2/3）

- `5486641` W4 真机挂接修复
- `35e0c4f` T1-T3 Android 三连
- `f2bb46f` W5/W6 批次2-1/2-2
- `847b3f2` W7/W8 批次3-8/3-10

### 验证记录（全部真实执行）

- **Windows 现役栈**：`validate_release.py` failures=0；ruff 全过；bandit 0 Medium/High；
  mypy（CI 同口径 19 文件）0 错误；8 个 selftest 全过（新增 native_core：
  pywebview 6.2.1 真实布局解析回归锁 + 门禁 fail-closed 行为锁 + 单源契约）
- **C# 栈**：`dotnet build` 0 警告 0 错误；`dotnet test` 27/27 通过
- **Android**：`:app:compileDebugKotlin` 通过；`:app:testDebugUnitTest` 12/12；
  ktlintCheck + detekt（app、webview-adapter 四项门禁）通过——由执行代理先行验证，
  主线对 SSL cancel（绝不 proceed）、桥全方法门禁、onDestroy isFinishing、
  下载净化四个安全点逐项复核后独立复跑 gradle 门禁确认
- **未验证项（诚实声明）**：Windows 端 GUI 实际运行（原生挂接在真实 WebView2
  上的行为）需真机/桌面冒烟（`--smoke-test`）确认；Android 需真机回归
  （错误页 UI、折叠屏变更、崩溃恢复）；这两项属 device-validation 范畴

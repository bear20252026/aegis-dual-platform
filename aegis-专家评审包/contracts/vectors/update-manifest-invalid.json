{
  "description": "蓝图阶段 B（contracts/vectors）：非法更新清单测试向量（回滚/签名不足/重复 key/过期/单数 signature——全部拒绝）",
  "vectors": [
    { "case": "rollback", "version": "0.9.0", "min_version": "1.0.0", "expected": "deny_rollback" },
    { "case": "threshold_insufficient", "signatures_count": 1, "threshold": 2, "expected": "deny_threshold" },
    { "case": "duplicate_key", "signatures": ["k1", "k1"], "threshold": 2, "expected": "deny_threshold" },
    { "case": "expired", "expires_at": "2020-01-01T00:00:00Z", "expected": "deny_expired" },
    { "case": "singular_signature_field", "uses_singular_signature": true, "expected": "deny_schema" }
  ]
}


# 字体下载 zip 中间产物（不入库；本地保留于 D:/software 与 assets/fonts）
assets/fonts/inter.zip
assets/fonts/source-han-sans-sc.zip
assets/fonts/**/*.otf
assets/fonts/inter/
assets/fonts/source-han-sans-sc/

# pyscn 工具输出目录（报告归档于 docs/quality-reports/，此处不重复入库）
.pyscn/

# 签名凭据（政府项目红线：绝不入库；example 为占位符可入库）
signing.properties
*.jks
*.keystore
*.p12

# skylos 工具输出目录（报告归档于 docs/quality-reports/，此处不重复入库）
.skylos/

# hotspottriage 工具输出目录（报告归档于 docs/quality-reports/，此处不重复入库）
.hotspottriage/

# 运行时日志目录（events.log 等，不入库）
windows/crash_reports/

# Python 缓存（不入库）
__pycache__/
*.pyc

# 构建产物（dist/——不入库，由 release.yml 构建）
dist/

# 生成的 MD 单文件（aegis-source-all.md——不入库）
aegis-source-all.md

# 生成的归档 zip（不入库）
aegis-source-2026-08-15.zip

# 生成的架构 MD 单文件（不入库）
aegis-source-architecture.md

# 审查材料 MD 生成产物（不入库）
aegis-review-materials.md

# windows_host 构建产物（.NET——不入库）
windows_host/bin/
windows_host/obj/

# windows/src 构建产物（.NET——不入库）
windows/src/**/bin/
windows/src/**/obj/

# Rust 构建产物（cargo target——不入库）
core/rust-policy-core/target/

# fuzz build artifacts
core/rust-policy-core/fuzz/target/

# Legacy Python (只读归档——不参与现役构建)
legacy/crash_reports/
legacy/windows-pywebview/dist/
legacy/windows-pywebview/build/
legacy/windows-pywebview/__pycache__/

# ── 仓库卫生修正（新增：此前未覆盖、已随历史提交入库的构建/交付产物）──
# .NET 生成产物（obj/bin——覆盖 windows 与评审包镜像；Rust 的 src/bin 为真实源码，不受影响）
**/obj/
**/tests/**/bin/
aegis-专家评审包/windows/**/bin/

# Android 构建产物（含评审包镜像）
**/build/

# 交付物（安装包 / 字体 bundle / 源码打包 zip）
**/docs/release/installer_output/
assets/fonts/fonts-bundle.zip
aegis-*.zip

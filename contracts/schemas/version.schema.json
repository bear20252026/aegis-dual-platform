{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Aegis Version（SemVer 版本——契约统一）",
  "description": "蓝图阶段 B（contracts 冻结）+ P0-04 修复：SemVer 字符串为唯一版本表示（update_verifier 与 schema 契约一致——不再整数/字符串混用）。（PY-001：pattern 补预发布/构建段——此前 2.2.0-beta.44 自身无法通过）",
  "type": "object",
  "required": [
    "version"
  ],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    }
  },
  "additionalProperties": false
}
